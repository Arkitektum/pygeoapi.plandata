name: Check vulnerabilities

on:
  push:
    paths-ignore:
      - '**.md'
  pull_request:
    branches:
      - master
    paths-ignore:
      - '!**.md'
  release:
    types:
      - released

jobs:

  vulnerabilities:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: .
    steps:
    - name: Checkout pygeoapi
      uses: actions/checkout@master
    - name: Scan vulnerabilities with trivy
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: fs
        exit-code: 1
        ignore-unfixed: true
        severity: CRITICAL,HIGH
        scanners: vuln,misconfig,secret
        scan-ref: .
    - name: Compute lowercase image reference
      # Docker/OCI references must be lowercase; github.repository contains
      # uppercase (Arkitektum/...), which trivy cannot parse.
      run: echo "IMAGE_REF=$(echo '${{ github.repository }}:${{ github.sha }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"
    - name: Build locally the image from Dockerfile
      run: |
        docker buildx build -t "$IMAGE_REF" --platform linux/amd64 --no-cache -f Dockerfile .
    - name: Scan locally built Docker image for vulnerabilities with trivy
      uses: aquasecurity/trivy-action@master
      env:
        TRIVY_DB_REPOSITORY: public.ecr.aws/aquasecurity/trivy-db:2
        TRIVY_JAVA_DB_REPOSITORY: public.ecr.aws/aquasecurity/trivy-java-db:1
      with:
        scan-type: image
        exit-code: 1
        ignore-unfixed: true
        severity: CRITICAL,HIGH
        vuln-type: os,library
        image-ref: '${{ env.IMAGE_REF }}'
