[pytest]
env =
    PYGEOAPI_CONFIG=pygeoapi-config.yml
    PYGEOAPI_OPENAPI=tests/pygeoapi-test-openapi.yml
markers =
    smoke: fork-gated end-to-end smoke; needs postgres + the Arkitektum packages (run with `pytest tests/smoke -m smoke`)
addopts = -m "not smoke"
